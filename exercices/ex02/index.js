
/**
 * export a function that takes a number (an age) and return true
 *  if this is old enough to drink, false otherwise
 */

function check (age) {
  if (age >= 18) {
    return true
  }
  return false
}

export default check
